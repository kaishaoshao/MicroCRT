/*
 * Compatibility wrapper.
 *
 * The real shared dispatch layer now lives under core/.
 * Keep this top-level include name stable while callers migrate.
 */

#include "core/printf_core_dispatch.inc"
